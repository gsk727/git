
如果安装了python，那么运行python server.py即可运行程序。

如果没有安装python，直接运行python2c.exe 即可。


pylibs 包含了程序运行需要的python相关的库，其中.so 文件是linux系统需要的动态库。
*.dll 是windows的
debug目录是一些的库的debug版本的，使用vs2010编译而成。

python2c.exe 启动服务器的windows版本

使用ctrl-c 关闭服务器。
无须安装python即可运行。